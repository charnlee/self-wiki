# Copy this file to .env and change the values before deployment.

# Use a real domain for HTTPS, for example: wiki.example.com
# Use :80 if you only want plain HTTP during testing.
SITE_HOST=:80

POSTGRES_DB=wiki
POSTGRES_USER=wikijs
POSTGRES_PASSWORD=change-this-password

LOG_LEVEL=info
LOG_FORMAT=default
